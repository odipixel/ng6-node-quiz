import { Route } from '@angular/router';
import { ComponentsComponent } from './index';

export const ComponentsRoutes: Route[] = [
    {
      path: 'components',
      component: ComponentsComponent
    }
];
